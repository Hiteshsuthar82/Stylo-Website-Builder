<!DOCTYPE html>
<html>
<head>
  <style>
    .container {
      max-width: 600px;
      margin: auto;
      padding: 20px;
      font-family: Arial, sans-serif;
      background-color: #f3f6fc;
      border-radius: 10px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      background: linear-gradient(90deg, #6a11cb, #2575fc);
      color: white;
      padding: 20px;
      border-radius: 10px 10px 0 0;
      font-size: 24px;
      font-weight: bold;
    }
    .sub-header {
      font-size: 16px;
      font-weight: normal;
      margin-top: 5px;
      opacity: 0.9;
    }
    .content {
      padding: 25px;
      background: white;
      text-align: center;
      border-radius: 0 0 10px 10px;
    }
    .credentials {
      background: #e1ebfd;
      padding: 15px 20px;
      border-radius: 8px;
      margin: 20px 0;
      text-align: left;
    }
    .credential-item {
      margin: 10px 0;
    }
    .credential-label {
      font-weight: bold;
      color: #444;
    }
    .credential-value {
      font-family: monospace;
      background: white;
      padding: 5px 10px;
      border-radius: 4px;
      display: inline-block;
      margin-left: 10px;
      border: 1px solid #ddd;
    }
    .action-button {
      display: inline-block;
      background: linear-gradient(90deg, #6a11cb, #2575fc);
      color: white;
      text-decoration: none;
      padding: 12px 25px;
      border-radius: 5px;
      font-weight: bold;
      margin: 20px 0 10px;
      transition: all 0.3s ease;
    }
    .action-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .note {
      font-size: 13px;
      color: #666;
      margin-top: 15px;
      font-style: italic;
    }
    .footer {
      text-align: center;
      padding: 15px;
      font-size: 12px;
      color: #555;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      Admin Invitation
      <div class="sub-header">Welcome to the Team</div>
    </div>
    <div class="content">
      <p>Hello <strong>{{fullName}}</strong>,</p>
      <p>You have been invited to join as an administrator. Below are your login credentials:</p>
      
      <div class="credentials">
        <div class="credential-item">
          <span class="credential-label">Username:</span>
          <span class="credential-value">{{username}}</span>
        </div>
        <div class="credential-item">
          <span class="credential-label">Email:</span>
          <span class="credential-value">{{email}}</span>
        </div>
        <div class="credential-item">
          <span class="credential-label">Password:</span>
          <span class="credential-value">{{password}}</span>
        </div>
        <div class="credential-item">
          <span class="credential-label">Role:</span>
          <span class="credential-value">{{role}}</span>
        </div>
      </div>
      
      <p>Please login using these credentials and then verify your email for full access.</p>
      
      <a href="{{loginUrl}}" class="action-button">Login Now</a>
      
      <p class="note">Important: After logging in, please go to your profile page and click on "Verify Now" to complete your account setup.</p>
      
      <p class="note">For security reasons, we recommend that change your password after your first login.</p>
    </div>
    <div class="footer">
      <p>&copy; 2024 Stylo Website Builder | All Rights Reserved</p>
      <p>This is an automated message, please do not reply.</p>
    </div>
  </div>
</body>
</html>